apiVersion: slo.ext.grafana.app/v1alpha1
kind: SLO
metadata:
  name: slolistcompanies
spec:
  name: "List Companies — Availability"
  description: "99.9% of GET /api/companies requests succeed (non-5xx) over a 28-day window"
  labels:
    - key: team
      value: mysql-react-app-demo
    - key: journey
      value: list-companies
  destinationDatasource:
    uid: grafanacloud-prom
  objectives:
    - value: 0.999
      window: 28d
  query:
    type: freeform
    freeform:
      query: >
        sum(rate(http_server_request_duration_seconds_count{
          http_route="/api/companies",
          http_request_method="GET",
          http_response_status_code!~"5.."
        }[$__rate_interval]))
        /
        sum(rate(http_server_request_duration_seconds_count{
          http_route="/api/companies",
          http_request_method="GET"
        }[$__rate_interval]))
