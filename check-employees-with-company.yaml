# IMPORTANT: Replace TARGET_URL with a publicly reachable URL before deploying.
# See check-health.yaml for instructions on using ngrok or similar tunneling.
apiVersion: syntheticmonitoring.ext.grafana.app/v1alpha1
kind: Check
metadata:
  name: employees-with-company-http
spec:
  job: "mysql-react-app-employees-join"
  target: "TARGET_URL/api/employees/with-company"
  frequency: 60000   # 60 seconds — JOIN query, heavier
  timeout: 10000
  enabled: true
  basicMetricsOnly: false
  alertSensitivity: medium
  probes: []
  settings:
    http:
      ipVersion: V4
      noFollowRedirects: false
      validStatusCodes:
        - 200
